apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "benchfinity.serviceAccountName" . }}
  labels:
    {{- include "benchfinity.labels" . | nindent 4 }}
{{- with .Values.imagePullSecrets }}
imagePullSecrets:
  {{- toYaml . | nindent 2 }}
{{- end }}
